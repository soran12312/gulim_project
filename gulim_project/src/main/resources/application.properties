# JSP view resolver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# mtbatis
# vo location
mybatis.type-aliases-package=project.gulim.domain

# mapper location
mybatis.mapper-locations=classpath:mappers/**/*.xml

# security
spring.config.import=security.properties


@Value("${file.upload.path}") // application.properties 또는 application.yml에서 경로 설정
private String fileUploadPath;

# 경로 사용 예시
String filePath = fileUploadPath + "/images/";